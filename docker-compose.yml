version: '3'
services:
  foo:
    build: ./foo
    ports:
      - "5000:5000"
    links:
      - bar
    environment:
      BAR_ENDPOINT: http://172.18.0.1:5001
  bar:
    build: ./bar
    ports:
      - "5001:5000"
